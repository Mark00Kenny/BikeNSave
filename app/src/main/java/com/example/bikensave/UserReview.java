/*

package com.example.bikensave;

import androidx.annotation.NonNull;
import androidx.appcompat.app.AppCompatActivity;
import androidx.recyclerview.widget.RecyclerView;

import android.os.Bundle;
import android.view.View;
import android.widget.ImageView;
import android.widget.TextView;

import com.google.firebase.database.DatabaseReference;
import com.google.firebase.database.FirebaseDatabase;
import com.google.firebase.storage.FirebaseStorage;
import com.google.firebase.storage.StorageReference;

public class UserReview extends AppCompatActivity {

    ImageView like_btn;
    TextView like_text;
    ImageView dislike_btn;
    TextView dislike_text;


    RecyclerView recyclerView;




    protected void onCreate (Bundle savedInstanceState) {

        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_user_review);

        recyclerView = findViewById(R.id.recyclerView);
        like_btn=(ImageView)itemView.findViewById(R.id.like_btn);
        like_text=(TextView)itemView.findViewById(R.id.like_text);

        dislike_btn=(ImageView)itemView.findViewById(R.id.dislike_btn);
        dislike_text=(TextView)itemView.findViewById(R.id.dislike_text);
    }
}
*/